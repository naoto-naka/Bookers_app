  <div class="col-md-offset-3 col-md-6">
	  <h1>Editing Book</h1>

	  <div class="error-format">
    	<% if @book.errors.any? %>
    	  <strong><%= @book.errors.count %>errors prohibited this obj from being saved:</strong><br>
    	  <% @book.errors.full_messages.each do |message| %>
    	      <%= "・#{message}" %><br>
    	      <% end %>
    	    <% end %>
</div>

	  <%= form_for(@book) do |f| %>
		<p>Title</p>
		<%= f.text_field :title,class: "col-md-12" %>
		<p>Opinion</p>
	    <%= f.text_area :body,class: "col-md-12" %><br>
	    <%= f.submit "Update Book",class: "btn btn-primary col-md-12 opinion-margin-bottom" %>
   	  <% end %>
   	  <%= link_to "Show", book_path(@book) %> | <%= link_to "Back",books_path %>
    </div>
 