<div class="col-md-4">

<div class="error-format">
    	<% if @book.errors.any? %>
    	  <strong><%= @book.errors.count %>errors prohibited this obj from being saved:</strong><br>
    	  <% @book.errors.full_messages.each do |message| %>
    	      <%= "・#{message}" %><br>
    	      <% end %>
    	    <% end %>
</div>

	  <%= render 'layouts/sidebar', user: current_user, book: @book %>
	</div>
  <h1>Books</h1>
  <div class="col-md-8">
    <table class="table table-striped">
	  <tr>
	   	<th></th>
		<th>Title</th>
		<th>Opinion</th>
	  </tr>
  <% @books.each do |book| %>
  	<tr>
	  <td>
	    <%= link_to user_path(book.id) do %>
		<%= attachment_image_tag book.user, :profile_image, :fill, 50, 50, fallback: "no_image.jpg", size: '50x50' %>
		<% end %>
	  </td>
	  <td><%= link_to book.title,book_path(book.id) %></td>
	  <td><%= book.body %></td>
	</tr>
  <% end %>
    </table>
  </div>

